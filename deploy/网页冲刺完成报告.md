# 网页冲刺完成报告

完成日期：2026-07-17  
范围：`web/`、部署配置与交付说明；未修改诊断管道、Pydantic 数据契约或 JSON 字段名。

## 1. 结果摘要

网页冲刺已完成三批独立提交：

| Commit | 结果 |
|---|---|
| `8b934d5` | 性能预算、零外部依赖、组件懒加载、静态预压缩 |
| `d4c39ef` | 国内动态版 / 国际静态版双构建与双部署指引 |
| `60c4683` | 六项评审证据、问题式叙事、URL 视角、回放控制、移动端与 404 |

最终页面在指定节流条件下 LCP 1.972s，初始 JS 36.37KB gzip，首屏 5 请求；三项均低于
2.5s / 300KB / 15 请求预算。国内、国际两套生产构建均已生成并通过本地预览。

## 2. 改了什么

### 性能与稳定性

- 删除 Google Fonts 和 jsDelivr ECharts 两个阻塞型外部依赖；
- 完整报告的固定 SOV 图改为轻量 CSS 图表，数据与无障碍描述保持一致；
- 技术视角、实况、原始 JSON 查看器改为异步组件；
- 首屏增加 CSS 骨架，并预加载 221KB 回放 JSON；
- HTML/CSS/JS/JSON 构建后生成 `.gz`，Nginx 启用 `gzip_static`；
- 维持系统字体栈，不增加 UI 库、图表库或运行时依赖。

### 评审体验

- Hero 用一句话解释产品，再给出“AI 认识 Deli，但不会主动推荐它”的核心洞察；
- 首屏展示无品牌词提及 0%、品牌词可见度 100%、官网西语内容 0 页和单轮样本边界；
- 新增六张“写给评审者”证据卡，逐项映射业务理解、拆解能力、范围控制、交付物、
  工程意识、独立判断；
- 新增范围与取舍、交付物、工程保障、限制与不确定性、下一步路线图；
- 页面按“这是什么 → 结论 → 怎么得出 → 证据 → 边界 → 下一步”组织；
- 产品视角提供人话解释，技术视角保留命令、字段、来源和按需原始 JSON。

### 交互与适配

- `?view=product` / `?view=technical` 可分享指定视角；
- 回放支持暂停、1×/2×、逐步和跳到结论；
- 390×844 手机竖屏无横向溢出；
- 新增 favicon、强化 title/meta、自包含 404；
- `/report/` 在国内与国际两套产物中均为直接可达的完整报告。

## 3. 体积与性能前后对比

| 项目 | 冲刺前 | 冲刺后 | 说明 |
|---|---:|---:|---|
| 初始 JS gzip | 32.08KB | 36.37KB | 增加完整评审叙事与 URL/回放交互，仍仅为预算的 12.1% |
| 初始 CSS gzip | 4.87KB | 6.32KB | 增加六卡、范围、交付与响应式布局 |
| 阻塞型外部依赖 | 2 类 | 0 | Google Fonts / ECharts CDN 均移除 |
| 首屏请求 | 未按同口径留档 | 5 | Lighthouse Network Requests |
| LCP | 未按同口径留档 | 1971.537ms | DevTools 实际节流、4× CPU、Fast 3G 下载侧、1440×900 |
| TBT / CLS | 未按同口径留档 | 122.739ms / 0.0014 | 记录值 |
| 首屏总传输 | 未按同口径留档 | 约 94KiB | 本地生产预览 |

原始机器报告与可视化报告：

- `web/performance/lighthouse-desktop-fast3g.report.json`
- `web/performance/lighthouse-desktop-fast3g.report.html`
- 测试口径：`web/performance/README.md`

视觉 QA：

- `web/qa/desktop-hero.png`
- `web/qa/reviewer-cards.png`
- `web/qa/mobile-390.png`

## 4. 两条部署线路

| 线路 | 产物 | 能力 | 部署 |
|---|---|---|---|
| 国内动态版 | `web/dist/` | 回放 + 完整报告 + 显式实况 `/api` | `deploy/部署手册.md` + ECS/Nginx |
| 国际静态版 | `web/dist-intl/` | 回放 + 完整报告；构建时关闭实况 | `deploy/Cloudflare Pages部署指引.md` + Direct Upload |

国际版构建不生成 LivePanel chunk，主资源未发现 `/api` 字面量；它不需要 FastAPI 或任何密钥。
Cloudflare Direct Upload 的当前控制台流程与限制以
[官方文档](https://developers.cloudflare.com/pages/get-started/direct-upload/)为准。

## 5. 最终验收

| 检查 | 结果 |
|---|---|
| `npm run build:all` | 国内 / 国际构建均通过 |
| Pytest | 55 passed |
| Mock 基线 | 20 问题 / 8 回答 / 7 缺口 / 7 建议 |
| 桌面布局 | 1280px 无横向溢出，六卡 3×2 |
| 手机布局 | 390×844 无横向溢出，主板块单列 |
| 交互 | URL 视角、2×、跳到结论、`/report/` 均浏览器实测通过 |
| 国际静态边界 | 实况禁用、零 `/api` 请求、无 LivePanel chunk |
| 控制台 | 国内 / 国际零 error、零 warning |
| 安全 | 未将 `.env` 或密钥写入构建；国际版纯静态 |

## 6. 遗留事项与移交

1. 用户执行 ECS 与 Cloudflare Pages 部署，获得两个公网 URL 后回填 `SUBMISSION.md`；
2. 请一位不懂技术的人做 10 秒/20 秒真人复述：Codex 已完成结构与截图代理检查，但不冒充真人受试者；
3. 公网上线后按同一节流口径复测 LCP，公网延迟、缓存与代理配置可能改变本机结果；
4. OpenAI 官方 credits 与 Gemini Grounding 配额仍是外部账户条件，不影响本次网页双线路交付；
5. 若国内版开启实况，仅在服务器 `.env` 放 DeepSeek Key，并按上线前检查单验证限流与超时。

除上述外部动作外，本轮网页冲刺无代码遗留项。

